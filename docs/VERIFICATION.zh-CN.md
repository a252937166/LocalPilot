# 0.7.6 验证范围

2026-09-14，发布目录执行 `.venv/bin/python scripts/check.py --browser`。两组旧测试分别缺少本地页面工作区配置和模拟页面的 frames 属性；补齐测试配置后仅重跑这两组，最终 **18 组、667/667 项断言通过**。记录见 [validation-0.7.6.json](validation-0.7.6.json)。总数是各层断言之和，不代表独立功能数量。

此次先验证提交的补丁（54/54），再补充修复链接路径、受限 frame、标签标题及截图/快照期间跳转检查。最终浏览器集为 66/66，新增不启动浏览器的 URL 和时序边界集为 25/25；会话持久化 8/8、模拟宿主面板 40/40 也通过。其他各组通过数与下方 0.7.5 表格一致。full_machine 的跨目录文件访问仍被允许，workspace 的保留路径、符号链接及硬链接被拒绝。

浏览器检查使用临时 Chrome 配置、本机测试页和合成文件，没有使用私人登录信息或调用模型 API。这些工具入口和结果检查不能证明 Chrome 进程获得了操作系统文件沙箱，也没有覆盖恶意网页的所有子资源或文件系统并发变更。此前普通 Chat 的验收记录仍属于 0.7.5；本次未重跑普通 Chat 一轮任务、原生生图或一小时无人干预执行。

版本同时包含 MCP 初始化图标元数据及 256×256、5,738 字节的网页专用 PNG。网页创建连接上传小图标并刷新后仍显示的结果已单独验证；更改本地 manifest 不会自动更新旧连接的图标。

## 0.7.5 历史验证

2026-09-12，在独立的发布工作目录重新安装依赖并执行 `.venv/bin/python scripts/check.py --browser`，**626/626 项断言通过**。这份总数包含不同层次的回归断言，不代表 626 个独立功能或缺陷。

| 检查 | 通过 |
|---|---:|
| 文件与 shell | 31 |
| 任务控制器 | 43 |
| 技能发现与加载 | 48 |
| 补丁与 MCP 升级 | 23 |
| 图片工作流 | 56 |
| 补丁及取消回归 | 38 |
| 历史状态回归 | 7 |
| 本轮故障边界 | 17 |
| 图片与桥接边界 | 24 |
| 图片读取 | 40 |
| 图片编辑及下载 | 96 |
| 原图传递 | 37 |
| 长作业边界 | 29 |
| 浏览器 | 50 |
| 浏览器会话持久化 | 8 |
| 综合能力回归 | 39 |
| 实际 Chrome 中的模拟宿主面板 | 40 |

汇总数据保存在 [validation-0.7.5.json](validation-0.7.5.json)。重新执行会在本机生成完整报告和日志，`verification/` 默认不进入 Git。面板检查包含多个超时与恢复情景，可能需要数分钟；浏览器检查使用独立临时配置，不依赖维护者的登录态。

0.7.5 修复的主要问题包括：图片暂停或超时后仍可能提交文件、未预期异常留下永久 running 回执、输出父目录创建被误拒、参考图依赖可淘汰缓存、MCP 配置变更后仍复用旧连接、发现结果暴露敏感启动参数、坏 MCP 条目影响健康服务。对应故障用例在 `verify_audit_edges.py`；实际 MCP 加延迟 HTTPS 图片响应的取消检查在 `verify_image_workflow.py`。

该 Agent 版本还在维护者的普通 Chat 中完成过“读取项目规则和技能引用 → 修改文本 → 旋转本机图片 → 执行原验证脚本 → 读回 → 结项”。磁盘及 SQLite 独立检查 16/16 通过，命令退出码 0，任务 4/4 步骤、8/8 固定验收通过。为了保护本机路径和私人会话，原始聊天记录与状态库不公开；README 展示的是明确标注的演示数据，不能替代这项维护者验收。

本次发布的 23 个 Agent 文件与该已部署 0.7.5 的文件逐一比对一致。运行辅助脚本、公开文档、示例数据和测试入口已单独整理；没有附带任何有效 App ID、Tunnel ID、密钥、浏览器配置或私人日志。发布文件经过凭据扫描。

本轮没有重新验证一小时普通 Chat 无人干预执行，也没有完成原生生成式改图保真度验收。长作业边界测试、短任务成功、参考图上传和图片保存，均不能代替这两项结果。当前支持范围是已测试的 macOS；其他系统需要补充执行、权限和浏览器验证。
